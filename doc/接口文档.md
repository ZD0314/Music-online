# 在线音乐播放器系统 - RESTful API 接口文档

## 1. 接口规范

### 1.1 统一响应格式

```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

**字段说明**:
- `code`: 状态码（200-成功，其他-失败）
- `message`: 响应消息
- `data`: 响应数据

### 1.2 分页响应格式

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [],
    "total": 100,
    "size": 20,
    "current": 1,
    "pages": 5
  }
}
```

### 1.3 错误码定义

| 错误码 | 说明 |
|--------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未认证 |
| 403 | 无权限 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |
| 1001 | 用户名或密码错误 |
| 1002 | 用户已被禁用 |
| 1003 | Token 已过期 |
| 1004 | 手机号已注册 |
| 1005 | 邮箱已注册 |
| 2001 | 文件类型不支持 |
| 2002 | 文件大小超限 |
| 2003 | 文件上传失败 |

### 1.4 请求头规范

```
Content-Type: application/json
Authorization: Bearer {token}
```

---

## 2. 认证相关接口

### 2.1 管理员登录

**接口**: `POST /api/admin/login`

**请求参数**:
```json
{
  "username": "admin",
  "password": "123456"
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "adminInfo": {
      "id": 1,
      "username": "admin",
      "nickname": "管理员"
    }
  }
}
```

### 2.2 用户注册

**接口**: `POST /api/user/register`

**请求参数**:
```json
{
  "username": "user001",
  "password": "123456",
  "phone": "13800138000",
  "email": "user@example.com"
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "注册成功",
  "data": {
    "userId": 1
  }
}
```

### 2.3 用户登录

**接口**: `POST /api/user/login`

**请求参数**:
```json
{
  "account": "13800138000",
  "password": "123456"
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "userInfo": {
      "id": 1,
      "username": "user001",
      "nickname": "音乐爱好者",
      "avatar": "http://example.com/avatar.jpg"
    }
  }
}
```

### 2.4 登出

**接口**: `POST /api/logout`

**请求头**: `Authorization: Bearer {token}`

**响应示例**:
```json
{
  "code": 200,
  "message": "登出成功",
  "data": null
}
```

---

## 3. 管理员端接口

### 3.1 数据统计

**接口**: `GET /api/admin/statistics`

**请求头**: `Authorization: Bearer {token}`

**响应示例**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "userCount": 10000,
    "artistCount": 500,
    "playlistCount": 2000,
    "todayActiveUsers": 1500
  }
}
```

### 3.2 用户管理

#### 3.2.1 查询用户列表

**接口**: `GET /api/admin/users`

**请求参数**:
- `page`: 页码（默认1）
- `size`: 每页数量（默认20）
- `keyword`: 搜索关键词（可选）

**响应示例**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "username": "user001",
        "nickname": "音乐爱好者",
        "phone": "13800138000",
        "email": "user@example.com",
        "status": 1,
        "createTime": "2026-03-01 10:00:00"
      }
    ],
    "total": 100,
    "size": 20,
    "current": 1,
    "pages": 5
  }
}
```

#### 3.2.2 禁用用户

**接口**: `PUT /api/admin/user/{id}/disable`

**请求头**: `Authorization: Bearer {token}`

**响应示例**:
```json
{
  "code": 200,
  "message": "禁用成功",
  "data": null
}
```

#### 3.2.3 启用用户

**接口**: `PUT /api/admin/user/{id}/enable`

**请求头**: `Authorization: Bearer {token}`

**响应示例**:
```json
{
  "code": 200,
  "message": "启用成功",
  "data": null
}
```

### 3.3 歌手管理

#### 3.3.1 创建歌手

**接口**: `POST /api/admin/artist`

**请求参数**:
```json
{
  "name": "周杰伦",
  "nameEn": "Jay Chou",
  "avatar": "http://example.com/avatar.jpg",
  "genre": "流行",
  "language": "华语",
  "initial": "Z",
  "introduction": "华语流行音乐天王..."
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "创建成功",
  "data": {
    "artistId": 1
  }
}
```

#### 3.3.2 查询歌手列表

**接口**: `GET /api/admin/artists`

**请求参数**:
- `page`: 页码
- `size`: 每页数量
- `keyword`: 搜索关键词（可选）
- `genre`: 流派筛选（可选）
- `language`: 语种筛选（可选）

**响应示例**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "name": "周杰伦",
        "nameEn": "Jay Chou",
        "avatar": "http://example.com/avatar.jpg",
        "genre": "流行",
        "language": "华语",
        "songCount": 150
      }
    ],
    "total": 50,
    "size": 20,
    "current": 1,
    "pages": 3
  }
}
```

#### 3.3.3 更新歌手信息

**接口**: `PUT /api/admin/artist/{id}`

**请求参数**:
```json
{
  "name": "周杰伦",
  "nameEn": "Jay Chou",
  "avatar": "http://example.com/avatar.jpg",
  "genre": "流行",
  "language": "华语",
  "introduction": "更新后的简介..."
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "更新成功",
  "data": null
}
```

#### 3.3.4 删除歌手

**接口**: `DELETE /api/admin/artist/{id}`

**请求头**: `Authorization: Bearer {token}`

**响应示例**:
```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

### 3.4 歌曲管理

#### 3.4.1 创建歌曲

**接口**: `POST /api/admin/song`

**请求参数**:
```json
{
  "name": "晴天",
  "artistId": 1,
  "albumId": 1,
  "cover": "http://example.com/cover.jpg",
  "audioUrl": "http://example.com/song.mp3",
  "lyricUrl": "http://example.com/lyric.lrc",
  "duration": 267,
  "fileSize": 5242880,
  "format": "MP3"
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "创建成功",
  "data": {
    "songId": 1
  }
}
```

#### 3.4.2 查询歌曲列表

**接口**: `GET /api/admin/songs`

**请求参数**:
- `page`: 页码
- `size`: 每页数量
- `artistId`: 歌手ID筛选（可选）
- `keyword`: 搜索关键词（可选）

**响应示例**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "name": "晴天",
        "artistName": "周杰伦",
        "albumName": "叶惠美",
        "cover": "http://example.com/cover.jpg",
        "duration": 267,
        "playCount": 10000,
        "status": 1
      }
    ],
    "total": 100,
    "size": 20,
    "current": 1,
    "pages": 5
  }
}
```

#### 3.4.3 更新歌曲信息

**接口**: `PUT /api/admin/song/{id}`

**请求参数**:
```json
{
  "name": "晴天",
  "artistId": 1,
  "albumId": 1,
  "cover": "http://example.com/cover.jpg",
  "lyricUrl": "http://example.com/lyric.lrc"
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "更新成功",
  "data": null
}
```

#### 3.4.4 删除歌曲

**接口**: `DELETE /api/admin/song/{id}`

**请求头**: `Authorization: Bearer {token}`

**响应示例**:
```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

### 3.5 歌单管理

#### 3.5.1 创建歌单

**接口**: `POST /api/admin/playlist`

**请求参数**:
```json
{
  "name": "华语经典",
  "cover": "http://example.com/cover.jpg",
  "description": "收录华语经典歌曲",
  "tags": "华语,经典,流行"
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "创建成功",
  "data": {
    "playlistId": 1
  }
}
```

#### 3.5.2 查询歌单列表

**接口**: `GET /api/admin/playlists`

**请求参数**:
- `page`: 页码
- `size`: 每页数量
- `keyword`: 搜索关键词（可选）

**响应示例**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "name": "华语经典",
        "cover": "http://example.com/cover.jpg",
        "songCount": 50,
        "playCount": 10000,
        "collectCount": 500
      }
    ],
    "total": 50,
    "size": 20,
    "current": 1,
    "pages": 3
  }
}
```

#### 3.5.3 向歌单添加歌曲

**接口**: `POST /api/admin/playlist/{id}/songs`

**请求参数**:
```json
{
  "songIds": [1, 2, 3, 4, 5]
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "添加成功",
  "data": null
}
```

#### 3.5.4 从歌单移除歌曲

**接口**: `DELETE /api/admin/playlist/{playlistId}/song/{songId}`

**请求头**: `Authorization: Bearer {token}`

**响应示例**:
```json
{
  "code": 200,
  "message": "移除成功",
  "data": null
}
```

### 3.6 文件上传

**接口**: `POST /api/admin/file/upload`

**请求参数**: `multipart/form-data`
- `file`: 文件
- `type`: 文件类型（audio/image/lyric）

**响应示例**:
```json
{
  "code": 200,
  "message": "上传成功",
  "data": {
    "url": "http://example.com/files/20260309/abc123.mp3",
    "fileName": "abc123.mp3",
    "fileSize": 5242880
  }
}
```

---

## 4. 用户端接口

### 4.1 歌手浏览

#### 4.1.1 查询歌手列表

**接口**: `GET /api/artists`

**请求参数**:
- `page`: 页码
- `size`: 每页数量
- `initial`: 首字母筛选（可选）
- `genre`: 流派筛选（可选）
- `language`: 语种筛选（可选）

**响应示例**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "name": "周杰伦",
        "nameEn": "Jay Chou",
        "avatar": "http://example.com/avatar.jpg",
        "genre": "流行",
        "language": "华语"
      }
    ],
    "total": 50,
    "size": 20,
    "current": 1,
    "pages": 3
  }
}
```

#### 4.1.2 查询歌手详情

**接口**: `GET /api/artist/{id}`

**响应示例**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "name": "周杰伦",
    "nameEn": "Jay Chou",
    "avatar": "http://example.com/avatar.jpg",
    "genre": "流行",
    "language": "华语",
    "introduction": "华语流行音乐天王...",
    "hotSongs": [
      {
        "id": 1,
        "name": "晴天",
        "cover": "http://example.com/cover.jpg",
        "duration": 267,
        "playCount": 10000
      }
    ],
    "albums": [
      {
        "id": 1,
        "name": "叶惠美",
        "cover": "http://example.com/cover.jpg",
        "releaseDate": "2003-07-31"
      }
    ]
  }
}
```

### 4.2 歌单浏览

#### 4.2.1 查询歌单列表

**接口**: `GET /api/playlists`

**请求参数**:
- `page`: 页码
- `size`: 每页数量
- `tags`: 标签筛选（可选）

**响应示例**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "name": "华语经典",
        "cover": "http://example.com/cover.jpg",
        "description": "收录华语经典歌曲",
        "songCount": 50,
        "playCount": 10000,
        "collectCount": 500
      }
    ],
    "total": 50,
    "size": 20,
    "current": 1,
    "pages": 3
  }
}
```

#### 4.2.2 查询歌单详情

**接口**: `GET /api/playlist/{id}`

**响应示例**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "name": "华语经典",
    "cover": "http://example.com/cover.jpg",
    "description": "收录华语经典歌曲",
    "tags": "华语,经典,流行",
    "songCount": 50,
    "playCount": 10000,
    "collectCount": 500,
    "songs": [
      {
        "id": 1,
        "name": "晴天",
        "artistName": "周杰伦",
        "albumName": "叶惠美",
        "cover": "http://example.com/cover.jpg",
        "duration": 267
      }
    ]
  }
}
```

### 4.3 歌曲相关

#### 4.3.1 查询歌曲详情

**接口**: `GET /api/song/{id}`

**响应示例**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "name": "晴天",
    "artistId": 1,
    "artistName": "周杰伦",
    "albumId": 1,
    "albumName": "叶惠美",
    "cover": "http://example.com/cover.jpg",
    "audioUrl": "http://example.com/song.mp3",
    "duration": 267,
    "playCount": 10000
  }
}
```

#### 4.3.2 获取歌词

**接口**: `GET /api/song/{id}/lyric`

**响应示例**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "lyric": "[00:00.00]晴天 - 周杰伦\n[00:10.00]故事的小黄花\n[00:12.50]从出生那年就飘着..."
  }
}
```

#### 4.3.3 增加播放次数

**接口**: `POST /api/song/{id}/play`

**请求头**: `Authorization: Bearer {token}` (可选)

**响应示例**:
```json
{
  "code": 200,
  "message": "success",
  "data": null
}
```

### 4.4 搜索功能

**接口**: `GET /api/search`

**请求参数**:
- `keyword`: 搜索关键词
- `type`: 搜索类型（all/song/artist/album，默认all）
- `page`: 页码
- `size`: 每页数量

**响应示例**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "songs": [
      {
        "id": 1,
        "name": "晴天",
        "artistName": "周杰伦",
        "cover": "http://example.com/cover.jpg"
      }
    ],
    "artists": [
      {
        "id": 1,
        "name": "周杰伦",
        "avatar": "http://example.com/avatar.jpg"
      }
    ],
    "albums": [
      {
        "id": 1,
        "name": "叶惠美",
        "artistName": "周杰伦",
        "cover": "http://example.com/cover.jpg"
      }
    ]
  }
}
```

### 4.5 收藏功能

#### 4.5.1 添加收藏

**接口**: `POST /api/favorite`

**请求头**: `Authorization: Bearer {token}`

**请求参数**:
```json
{
  "targetId": 1,
  "targetType": 1
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "收藏成功",
  "data": null
}
```

#### 4.5.2 取消收藏

**接口**: `DELETE /api/favorite`

**请求头**: `Authorization: Bearer {token}`

**请求参数**:
```json
{
  "targetId": 1,
  "targetType": 1
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "取消收藏成功",
  "data": null
}
```

#### 4.5.3 查询我的收藏

**接口**: `GET /api/favorites`

**请求头**: `Authorization: Bearer {token}`

**请求参数**:
- `targetType`: 目标类型（1-歌曲 2-歌单）
- `page`: 页码
- `size`: 每页数量

**响应示例**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "name": "晴天",
        "artistName": "周杰伦",
        "cover": "http://example.com/cover.jpg",
        "createTime": "2026-03-09 10:00:00"
      }
    ],
    "total": 50,
    "size": 20,
    "current": 1,
    "pages": 3
  }
}
```

### 4.6 个人中心

#### 4.6.1 查询个人信息

**接口**: `GET /api/user/info`

**请求头**: `Authorization: Bearer {token}`

**响应示例**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "username": "user001",
    "nickname": "音乐爱好者",
    "phone": "13800138000",
    "email": "user@example.com",
    "avatar": "http://example.com/avatar.jpg",
    "signature": "热爱音乐，热爱生活"
  }
}
```

#### 4.6.2 更新个人信息

**接口**: `PUT /api/user/info`

**请求头**: `Authorization: Bearer {token}`

**请求参数**:
```json
{
  "nickname": "音乐爱好者",
  "avatar": "http://example.com/avatar.jpg",
  "signature": "热爱音乐，热爱生活"
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "更新成功",
  "data": null
}
```

#### 4.6.3 修改密码

**接口**: `PUT /api/user/password`

**请求头**: `Authorization: Bearer {token}`

**请求参数**:
```json
{
  "oldPassword": "123456",
  "newPassword": "654321"
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "修改成功",
  "data": null
}
```

#### 4.6.4 创建个人歌单

**接口**: `POST /api/user/playlist`

**请求头**: `Authorization: Bearer {token}`

**请求参数**:
```json
{
  "name": "我的最爱",
  "cover": "http://example.com/cover.jpg",
  "description": "我最喜欢的歌曲",
  "status": 1
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "创建成功",
  "data": {
    "playlistId": 1
  }
}
```

#### 4.6.5 查询我的歌单

**接口**: `GET /api/user/playlists`

**请求头**: `Authorization: Bearer {token}`

**响应示例**:
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "name": "我的最爱",
      "cover": "http://example.com/cover.jpg",
      "songCount": 20,
      "createTime": "2026-03-09 10:00:00"
    }
  ]
}
```

---

## 5. 接口调用示例

### 5.1 用户听歌完整流程

```javascript
// 1. 用户登录
const loginRes = await axios.post('/api/user/login', {
  account: '13800138000',
  password: '123456'
});
const token = loginRes.data.data.token;

// 2. 设置请求头
axios.defaults.headers.common['Authorization'] = `Bearer ${token}`;

// 3. 浏览歌单
const playlistsRes = await axios.get('/api/playlists', {
  params: { page: 1, size: 20 }
});

// 4. 查看歌单详情
const playlistDetailRes = await axios.get('/api/playlist/1');

// 5. 播放歌曲
const songRes = await axios.get('/api/song/1');
const audioUrl = songRes.data.data.audioUrl;

// 6. 获取歌词
const lyricRes = await axios.get('/api/song/1/lyric');
const lyric = lyricRes.data.data.lyric;

// 7. 增加播放次数
await axios.post('/api/song/1/play');

// 8. 收藏歌曲
await axios.post('/api/favorite', {
  targetId: 1,
  targetType: 1
});
```

### 5.2 管理员上传歌曲流程

```javascript
// 1. 管理员登录
const loginRes = await axios.post('/api/admin/login', {
  username: 'admin',
  password: '123456'
});
const token = loginRes.data.data.token;

// 2. 设置请求头
axios.defaults.headers.common['Authorization'] = `Bearer ${token}`;

// 3. 创建歌手
const artistRes = await axios.post('/api/admin/artist', {
  name: '周杰伦',
  nameEn: 'Jay Chou',
  genre: '流行',
  language: '华语',
  initial: 'Z'
});
const artistId = artistRes.data.data.artistId;

// 4. 上传音频文件
const audioFormData = new FormData();
audioFormData.append('file', audioFile);
audioFormData.append('type', 'audio');
const audioRes = await axios.post('/api/admin/file/upload', audioFormData);
const audioUrl = audioRes.data.data.url;

// 5. 上传歌词文件
const lyricFormData = new FormData();
lyricFormData.append('file', lyricFile);
lyricFormData.append('type', 'lyric');
const lyricRes = await axios.post('/api/admin/file/upload', lyricFormData);
const lyricUrl = lyricRes.data.data.url;

// 6. 创建歌曲
await axios.post('/api/admin/song', {
  name: '晴天',
  artistId: artistId,
  audioUrl: audioUrl,
  lyricUrl: lyricUrl,
  duration: 267,
  format: 'MP3'
});
```

---

**文档版本**: v1.0
**编写日期**: 2026-03-09
**编写人**: Claude
**审核状态**: 待审核
