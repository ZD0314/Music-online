# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目概述

这是一个基于 Spring Boot 的在线音乐播放器系统，采用 B/S 架构，包含管理员端和用户端两大模块。

**技术栈**:
- 后端: Java + Spring Boot 2.7 + MyBatis-Plus
- 数据库: MySQL 8.0
- 前端: Vue.js / HTML5 + CSS3 + jQuery
- 文件存储: 本地磁盘 / MinIO / 阿里云 OSS

**当前状态**: 项目处于需求分析和架构设计阶段，尚未开始代码实现。

## 项目文档

核心文档位于 `doc/` 目录：

- `需求分析.md` - 完整的功能需求和非功能需求分析
- `需求.md` - 原始需求描述
- `task_plan.md` - 文档设计任务计划
- `findings.md` - 项目分析记录
- `progress.md` - 项目进度跟踪

**重要**: 在开始任何开发工作前，必须先阅读 `需求分析.md` 了解完整的业务需求。

## 系统架构

### 核心业务模块

**管理员端 (5个模块)**:
1. 登录系统 - BCrypt 加密认证
2. 数据统计 - 用户数、歌手数、歌单数、活跃度统计
3. 用户管理 - 查看、禁用、启用用户账号
4. 歌单管理 - 创建官方歌单、配置封面、管理歌曲列表
5. 歌手管理 - 维护歌手信息、上传音乐文件和歌词

**用户端 (6个模块)**:
1. 身份验证 - 手机号/邮箱注册登录
2. 歌手浏览 - 按首字母/语种/流派分类浏览
3. 歌单浏览 - 精选歌单、风格筛选、收藏
4. 搜索功能 - 歌曲/歌手/专辑模糊搜索
5. 音乐播放器 - 播放控制、模式切换（单曲/列表/随机）、歌词同步
6. 个人中心 - 信息修改、收藏管理、歌单创建

### 核心实体关系

```
用户 (User)
  ├─ 收藏歌曲 (Favorite Songs)
  └─ 创建歌单 (Created Playlists)

歌手 (Artist)
  ├─ 专辑 (Albums)
  └─ 歌曲 (Songs)

歌单 (Playlist)
  └─ 歌曲列表 (Song List)

歌曲 (Song)
  ├─ 音频文件 (MP3/FLAC)
  ├─ 歌词文件 (LRC)
  └─ 所属歌手/专辑
```

## 开发规范

### 代码规范
- 遵循阿里巴巴 Java 开发手册
- 关键业务逻辑必须添加注释
- 使用 BCrypt 加密存储密码
- 采用 JWT 或 Session 进行身份认证

### 安全要求
- 防止 SQL 注入、XSS 攻击
- 文件上传需验证类型和大小
- 敏感接口需验证用户身份和权限
- 接口需进行频率限制

### 文件格式约束
- 音频文件: 仅支持 MP3 和 FLAC
- 歌词文件: 仅支持 LRC 格式
- 图片文件: JPG/PNG，歌手头像 ≤1MB，歌单封面 ≤2MB

### API 设计规范
- 采用 RESTful API 规范
- 数据格式使用 JSON
- 统一响应格式（状态码、消息、数据）
- 支持分页查询

## 性能要求

- 支持至少 1000 并发用户在线
- 页面响应时间 ≤ 2 秒
- 音乐播放延迟 ≤ 1 秒
- 数据库查询响应时间 ≤ 500ms

## 开发注意事项

1. **层级关系**: 建立"歌手-专辑-歌曲"的层级关系
2. **角色分离**: 管理员和用户权限严格分离
3. **文件存储**: 开发阶段可使用本地存储，生产环境建议使用 OSS
4. **播放模式**: 支持单曲循环、列表循环、随机播放三种模式
5. **歌词同步**: 需解析 LRC 格式实现歌词滚动同步
6. **搜索功能**: 支持歌曲名称、歌手姓名、专辑名称的模糊搜索